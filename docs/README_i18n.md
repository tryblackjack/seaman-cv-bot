# 🌍 Мультиязычность в CV Distribution Bot

## Что было добавлено

### ✅ Основные возможности

1. **Автоматическое определение языка** - бот определяет язык из настроек Telegram пользователя при первом запуске
2. **Поддержка 3 языков:**
   - 🇬🇧 Английский (English) - по умолчанию
   - 🇷🇺 Русский (Russian)
   - 🇺🇦 Украинский (Ukrainian)

3. **Смена языка в любой момент:**
   - Команда `/language`
   - Кнопка "🌍 Сменить язык" в меню /start

4. **Полная локализация:**
   - Все сообщения бота
   - Кнопки меню
   - Инлайн клавиатуры
   - Сообщения об ошибках

## 📁 Структура файлов

```
project/
├── local_test_bot_i18n.py      # Основной файл бота с i18n
├── i18n_en.json                # Английские переводы
├── i18n_ru.json                # Русские переводы
├── i18n_uk.json                # Украинские переводы
├── recruiter_vessel_map.json   # База данных крюингов
└── temp_cvs/                   # Временные CV файлы
```

## 🚀 Установка и запуск

### 1. Подготовка файлов

Поместите все файлы в одну директорию:
- `local_test_bot_i18n.py` (основной бот)
- `i18n_en.json`, `i18n_ru.json`, `i18n_uk.json` (переводы)

### 2. Установка зависимостей

```bash
pip install python-telegram-bot aiohttp pypdf
```

### 3. Запуск бота

```bash
python local_test_bot_i18n.py
```

## 🔧 Как это работает

### Определение языка при старте

```python
# При команде /start:
1. Бот получает language_code из Telegram (например, "ru", "uk", "en")
2. Сохраняет язык в context.user_data['language']
3. Все последующие сообщения используют этот язык
```

### Функция t() - Translation Helper

```python
# Использование в коде:
await update.message.reply_text(t(context, 'start_welcome'))

# С параметрами:
await update.message.reply_text(
    t(context, 'targets_ready', count=10)
)
```

### Пример из i18n файла:

```json
{
  "start_welcome": "👋 Welcome! I'm a CV distribution bot.",
  "targets_ready": "🎯 Ready to send: {count} companies."
}
```

## 📝 Добавление нового языка

### Шаг 1: Создайте файл перевода

Создайте файл `i18n_es.json` (например, для испанского):

```json
{
  "language_name": "Español",
  "language_code": "es",
  "start_welcome": "👋 ¡Hola! Soy un bot de distribución de CV.",
  ...
}
```

### Шаг 2: Добавьте в список поддерживаемых языков

В `local_test_bot_i18n.py`:

```python
SUPPORTED_LANGUAGES = ['en', 'ru', 'uk', 'es']  # добавили 'es'
```

### Шаг 3: Добавьте кнопку в меню выбора языка

```python
async def language_command(update: Update, context: ContextTypes.DEFAULT_TYPE):
    keyboard = InlineKeyboardMarkup([
        [InlineKeyboardButton("🇬🇧 English", callback_data='lang_en')],
        [InlineKeyboardButton("🇷🇺 Русский", callback_data='lang_ru')],
        [InlineKeyboardButton("🇺🇦 Українська", callback_data='lang_uk')],
        [InlineKeyboardButton("🇪🇸 Español", callback_data='lang_es')]  # новая кнопка
    ])
```

## 🎯 Команды бота

- `/start` - Запуск бота (автоопределение языка)
- `/start_apply` - Начать процесс рассылки CV
- `/language` - Сменить язык интерфейса
- `/cancel` - Отменить текущую операцию

## 🔍 Как пользователь меняет язык

### Вариант 1: При старте бота

```
Пользователь: /start

Бот (на языке пользователя из Telegram):
"👋 Привет! Я бот для рассылки CV..."
[🌍 Сменить язык]  ← нажать эту кнопку
```

### Вариант 2: Команда /language

```
Пользователь: /language

Бот:
"🌍 Выберите ваш язык:
Текущий: Русский"

[🇬🇧 English]
[🇷🇺 Русский]  ← выбрать нужный
[🇺🇦 Українська]
```

## 📊 Что переведено

### ✅ Полностью локализовано:

- Приветственные сообщения
- Меню и кнопки
- Запросы данных (email, CV, должность, предпочтения)
- Процесс рассылки (прогресс, успех, ошибки)
- Сообщения об оплате
- Админ панель
- Все уведомления и статусы

### ✉️ Email тела (AI-генерируемые):

Email тела генерируются AI (Ollama) **на английском** по умолчанию, так как:
- Крюинговые компании обычно принимают CV на английском
- Это международный язык морской индустрии

Если нужна локализация email-тел, можно добавить промпт с указанием языка в функцию `analyze_cv_and_preferences()`.

## 🛠️ Редактирование переводов

### Чтобы изменить текст:

1. Откройте нужный файл (например, `i18n_ru.json`)
2. Найдите ключ перевода (например, `"start_welcome"`)
3. Измените текст
4. Сохраните файл
5. Перезапустите бота

### Пример:

**До:**
```json
"start_welcome": "👋 Привет! Я бот для рассылки CV."
```

**После:**
```json
"start_welcome": "👋 Здравствуйте! Я ваш персональный CV-ассистент."
```

## 📱 Пример работы

### Русский пользователь:

```
👤 Пользователь открывает бота
🤖 Бот: "👋 Привет! Я бот для рассылки CV моряков..."
           [🌍 Сменить язык]

👤 /start_apply
🤖 "Оплата услуги рассылки CV: 50 EUR."
   [💳 Оплатить (Тест)]
   [🔐 Админ доступ]

👤 [нажимает админ доступ]
🤖 "🔐 Введите админ пароль:"

👤 CaptainPass123
🤖 "✅ Пароль принят."
🤖 "📧 Введите ваш личный Email..."
```

### Английский пользователь (если язык Telegram = EN):

```
👤 User opens bot
🤖 Bot: "👋 Welcome! I'm a CV distribution bot..."
         [🌍 Change Language]

👤 /start_apply
🤖 "Payment for CV distribution service: 50 EUR."
   [💳 Pay (Test)]
   [🔐 Admin Access]
```

## 🎨 Технические детали

### Структура JSON файла перевода:

```json
{
  "language_name": "Русский",          // Название языка
  "language_code": "ru",               // ISO код
  
  "key_name": "Текст сообщения",       // Простой текст
  
  "key_with_params": "У вас {count} писем"  // С параметрами
}
```

### Использование параметров:

```python
# В коде:
t(context, 'targets_ready', count=10)

# В JSON:
"targets_ready": "🎯 Ready to send: {count} companies."

# Результат:
"🎯 Ready to send: 10 companies."
```

## 🐛 Отладка

### Логи показывают выбранный язык:

```
2024-01-15 10:30:00 - INFO - 🌍 Определен язык из Telegram: ru
2024-01-15 10:30:00 - INFO - 🌍 Язык установлен: ru
```

### Если перевод не работает:

1. Проверьте, что файл `i18n_XX.json` находится в той же папке что и бот
2. Проверьте JSON на валидность (можно использовать jsonlint.com)
3. Проверьте логи бота на наличие ошибок загрузки
4. Убедитесь что язык добавлен в `SUPPORTED_LANGUAGES`

## ⚙️ Настройки по умолчанию

```python
# В начале local_test_bot_i18n.py:

SUPPORTED_LANGUAGES = ['en', 'ru', 'uk']  # Список языков
DEFAULT_LANGUAGE = 'en'                    # Язык по умолчанию
```

## 📋 Checklist внедрения

- [x] Создать JSON файлы переводов (en, ru, uk)
- [x] Добавить функции загрузки и работы с переводами
- [x] Заменить все hardcoded тексты на вызовы t()
- [x] Добавить автоопределение языка из Telegram
- [x] Добавить команду /language
- [x] Добавить инлайн кнопки выбора языка
- [x] Протестировать все состояния ConversationHandler
- [x] Проверить форматирование с параметрами
- [x] Добавить логирование выбора языка

## 🎉 Готово!

Теперь ваш бот полностью мультиязычный и автоматически подстраивается под язык пользователя Telegram!

---

**Вопросы или нужна помощь?** Пишите!
